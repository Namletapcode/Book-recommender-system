// Error reporting stub — Lovable đã bị xóa
// Thay thế bằng console.error thuần túy

export function reportLovableError(error: unknown, _context: Record<string, unknown> = {}) {
  console.error('[BRS Error]', error);
}
